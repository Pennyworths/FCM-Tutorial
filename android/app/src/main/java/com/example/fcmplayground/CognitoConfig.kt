package com.example.fcmplayground

object CognitoConfig {
    // Cognito configuration values (from local.properties)
    // These values are loaded at build time from local.properties
    const val USER_POOL_ID = "us-east-1_prtCLdpeA"
    const val CLIENT_ID = "k9okus8oobca0gl489s7qh2cu"
    const val REGION = "us-east-1"
    const val DOMAIN = "fcm-dev-c800b6d9"
}
