# Environment Variables for FCM Tutorial Deployment
# Copy this file to .env and fill in your values
# 
# Usage:
#   cp env.example .env
#   # Edit .env with your actual values
#   ./deploy.sh

# Database Credentials (required)
# These will be used for both RDS and Lambda functions
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password

# Optional: If you want different credentials for Lambda
# RDS_USERNAME=your_rds_username
# RDS_PASSWORD=your_rds_password

# FCM Service Account JSON (required - choose one method)
# 
# ⭐ RECOMMENDED: If service-account.json is in project root, leave this unset
#    The script will automatically detect it
# FCM_SERVICE_ACCOUNT_JSON_FILE=

# Alternative: Use relative path (relative to project root)
# FCM_SERVICE_ACCOUNT_JSON_FILE=service-account.json

# Alternative: Use absolute path (not recommended - not portable)
# FCM_SERVICE_ACCOUNT_JSON_FILE=/absolute/path/to/service-account.json

# Alternative: Provide JSON content directly (not recommended for .env file)
# FCM_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'

# AWS Configuration (optional, defaults shown)
# AWS_REGION=us-east-1
# AWS_PROFILE=terraform

